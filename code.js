// encontrar una letra o vacal en una frase
/* var cadena = prompt("escribe una frase") 
var indices = [];
for(var i = 0; i < cadena.length; i++) {
	if (cadena[i].toLowerCase() === "o") {
        indices.push(i);
          }
}
document.write(`la frase contiene ${indices.length} letras O
`)
 */

 
